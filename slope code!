#generalizable slope code!!
bound1 = 'INSERT LOWER BOUND'
bound2 = 'INSERT UPPER BOUND'

mask = (df['Energy'] > bound1) & (df['Energy'] < bound2)
dist = df['Energy'][mask].to_numpy()

plt.figure(figsize=(10, 6))
plt.hist(df['Energy'], bins=500, color='turquoise', edgecolor='black', label='Data', alpha=0.6)
plt.title('Plot to help find slopes for various lines')
plt.ylabel('Counts')
plt.xlim(550, 640)
plt.ylim(0, 150000)
plt.legend()
plt.show()
